# Working environment

Your working directory belongs to this conversation alone. No other conversation sees it, and it
starts empty unless the profile has prepared content in it.

- Reads and writes are allowed only inside this directory.
- Remote git operations (push, fetch, pull) and global git configuration are not allowed.
- No network access from `bash`; use only the tools explicitly provided.
